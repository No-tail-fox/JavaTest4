server:
  port: ${port:18080}
#ø™∑¢≈‰÷√
spring:
  datasource:
    driverClassName : com.mysql.jdbc.Driver
    url: jdbc:mysql://${ip:47.100.57.92}:${port:3306}/sakila?useUnicode=true&characterEncoding=utf-8
    username: ${name:root}
    password: ${password:root}

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: Exam1.domain
